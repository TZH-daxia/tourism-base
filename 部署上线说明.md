# Tourism Base 部署上线说明

本文档用于说明 `tourism_base` 项目从本地开发环境部署到线上网站的大致流程。
当前项目包含以下几部分：

- 前端：`Vue 3 + Vite`
- 后端：`FastAPI`
- 数据库：`MongoDB`
- 向量库：`Milvus`
- 对象存储：`MinIO`
- 模型能力：OpenAI 兼容接口 + 本地检索链路

---

## 1. 上线目标

上线后的典型访问方式：

- 用户访问：`https://你的域名`
- 前端静态资源：由 `Nginx` 提供
- 后端接口：通过 `Nginx` 转发到 `FastAPI`
- 前端请求接口路径：`/api/...`

推荐结构：

```text
用户浏览器
    ↓
  Nginx
  ├─ /        -> 前端 dist
  └─ /api/    -> FastAPI(127.0.0.1:8000)
                 ├─ MongoDB
                 ├─ Milvus
                 └─ MinIO
```

---

## 2. 推荐部署方案

### 方案一：单机部署

适合首次上线、成本低、方便排查。

一台 Linux 云服务器中运行：

- `Nginx`
- `FastAPI`
- `MongoDB`
- `Milvus`
- `MinIO`

推荐配置：

- 测试环境：`4C8G`
- 正式环境：`8C16G` 或以上

### 方案二：Docker Compose 部署

适合后续维护和迁移。

建议：

- `MongoDB / Milvus / MinIO` 用 `Docker Compose`
- `Nginx` 和 `FastAPI` 可选宿主机直装，或一并容器化

如果后面你准备正式部署，优先推荐这个方案。

---

## 3. 上线前需要准备什么

### 3.1 云服务器

建议系统：

- `Ubuntu 22.04 LTS`

建议开放端口：

- `80`：HTTP
- `443`：HTTPS
- `22`：SSH

如果数据库和对象存储不对公网开放，就不要开放它们的端口。

### 3.2 域名

准备一个域名，例如：

- `tourism.example.com`

然后把域名解析到你的服务器公网 IP。

### 3.3 项目代码

你当前项目已经在 GitHub / Gitee 上，可以直接在服务器拉代码：

```bash
git clone git@github.com:你的用户名/tourism-base.git
```

---

## 4. 后端部署流程

### 4.1 安装 Python 和 uv

建议 Python 版本：

- `Python 3.11`

安装依赖：

```bash
cd tourism_base
uv sync
```

### 4.2 配置环境变量

把本地 `.env` 改成线上版本，重点检查这些配置：

- `MONGO_URL`
- `MONGO_DB_NAME=tb001`
- `MILVUS_HOST`
- `MILVUS_PORT`
- `MINIO_ENDPOINT`
- `MINIO_ACCESS_KEY`
- `MINIO_SECRET_KEY`
- `MINIO_BUCKET`
- `OPENAI_API_KEY`
- `OPENAI_BASE_URL`
- `OPENAI_MODEL`

建议做法：

- 保留项目中的 `.env`
- 线上单独维护 `.env.production` 或服务器专用 `.env`

### 4.3 启动后端

先手工验证启动：

```bash
python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

启动成功后验证：

- `http://127.0.0.1:8000/docs`
- `http://127.0.0.1:8000/api/knowledge/tasks`

### 4.4 使用 systemd 托管后端

建议不要长期手工跑，生产环境用 `systemd`。

示例思路：

- 服务名：`tourism-base.service`
- 工作目录：项目根目录
- 启动命令：`python -m uvicorn main:app --host 127.0.0.1 --port 8000`

这样可以实现：

- 开机自启
- 崩溃自动重启
- 统一日志管理

---

## 5. 前端部署流程

### 5.1 构建前端

```bash
cd web
npm install
npm run build
```

构建完成后会生成：

```text
web/dist
```

### 5.2 部署前端静态文件

把 `web/dist` 交给 `Nginx` 提供访问。

用户访问域名时，Nginx 返回前端页面；当前端请求 `/api/...` 时，再转发给 FastAPI。

---

## 6. Nginx 反向代理思路

Nginx 主要做两件事：

### 6.1 前端静态资源

- `/` 指向 `web/dist`

### 6.2 后端接口代理

- `/api/` 转发到 `http://127.0.0.1:8000`

这样前端代码里继续使用：

```text
/api/knowledge/tasks
```

就不需要把接口地址写死成某个 IP。

---

## 7. MongoDB / Milvus / MinIO 的部署建议

### 7.1 MongoDB

作用：

- 保存聊天会话
- 保存知识库导入任务

当前关键库名：

- `tb001`

关键集合：

- `chat_messages`
- `import_tasks`
- `counters`

### 7.2 Milvus

作用：

- 存储向量数据
- 支撑知识库检索

这是项目里最重的服务之一，建议优先用官方推荐方式部署，不要手搓。

### 7.3 MinIO

作用：

- 存储上传文件、图片等资源

上线时要特别确认：

- bucket 已创建
- 后端能访问 MinIO
- 前端图片走的是代理接口，不是直接暴露 MinIO 内网地址

---

## 8. HTTPS 配置

正式上线建议必须启用 HTTPS。

常见方式：

- `Nginx + Let's Encrypt`

好处：

- 浏览器不报不安全
- 登录页、上传、接口调用更稳定
- 便于以后接更多前端能力

---

## 9. 上线后的检查清单

建议至少验证下面这些点：

### 9.1 前端页面

- 首页能打开
- 登录弹窗正常
- 聊天页能进入

### 9.2 后端接口

- `/api/knowledge/tasks` 正常返回
- `/api/knowledge/recommendations` 正常返回
- `/api/knowledge/chat/sessions` 正常返回

### 9.3 聊天能力

- 能发消息
- 会话能新增
- 会话能删除、重命名
- 会话排序正常

### 9.4 知识库后台

- 历史任务能显示
- 上传文件能入库
- 删除任务正常
- 过程跟踪可展开/折叠

### 9.5 资源访问

- 图片能显示
- MinIO 资源代理正常

---

## 10. 常见问题

### 10.1 页面能打开，但知识库后台没数据

先查这几项：

- 后端是不是当前版本代码
- `8000` 端口是不是跑了旧进程
- `/api/knowledge/tasks` 是不是返回 `404`
- MongoDB 的 `tb001.import_tasks` 是否有数据

### 10.2 前端能打开，但接口报错

先确认：

- Nginx 是否把 `/api` 正确转发到后端
- 后端服务是否正常运行
- `.env` 里的数据库和模型配置是否正确

### 10.3 图片不显示

重点检查：

- MinIO 配置
- bucket 是否存在
- 后端资源代理接口是否可用

---

## 11. 建议的正式落地顺序

如果你后面真的要上线，建议按这个顺序推进：

1. 先准备一台 Linux 服务器
2. 部署 MongoDB / Milvus / MinIO
3. 配好项目 `.env`
4. 手工启动后端，确认接口通
5. 构建前端并交给 Nginx
6. 配置 `/api` 反向代理
7. 接入域名和 HTTPS
8. 做全链路验收

---

## 12. 后续可以继续补的文件

当你准备正式上线时，可以继续补这些内容：

- `docker-compose.yml`
- `nginx.conf`
- `systemd service` 文件
- 生产环境 `.env.example`
- 一键部署脚本

如果你之后说一声“开始做上线部署文件”，就可以直接基于这份说明继续往下生成。
