# 旅游知识库智能问答系统 - 项目需求文档 (PRD)

**文档版本**：v1.1  
**最后更新**：2026-05-20  
**维护团队**：旅游知识库项目组  
**适用对象**：产品经理、架构师、后端/算法开发、前端开发、测试工程师、旅游运营人员

---

## 1. 项目概述

### 1.1 项目背景
面向旅游垂直行业，建设一套企业级智能旅游知识库系统。系统需接入景区攻略、旅游线路、酒店信息、美食推荐、交通指南、文化民俗等多源异构文档，提供景点介绍、内容检索、线路推荐、实用信息查询与智能问答能力，服务于游客、导游及旅游运营人员。

### 1.2 项目定位
基于 LangGraph 工作流引擎构建的旅游垂直领域知识库管理系统。支持多格式文档异步解析、旅游实体抽取、向量化存储、多路混合召回检索与流式问答输出。系统强调**可追溯、可解释、高可用、易扩展**，为后续行程规划、多租户管理、多模态检索奠定架构基础。

### 1.3 核心价值
| 价值维度                 | 说明                                                         |
| ------------------------ | ------------------------------------------------------------ |
| **自动化导入与任务追踪** | 支持 PDF/Markdown 异步解析，内置导入任务状态机，失败隔离不阻塞主服务，支持进度推送与人工复核队列 |
| **旅游实体与意图识别**   | LLM 精准识别景区、线路、酒店、美食、地区等实体，结合对话历史解决模糊指代与多轮上下文关联 |
| **多路混合检索策略**     | 稠密向量 + 稀疏向量 + HyDE 假设检索 + MCP 实时搜索（天气/门票/交通），提升长尾与动态问题召回率 |
| **精准排序与引用溯源**   | RRF 倒排秩融合 + Reranker 二次精排，答案生成强制携带来源引用（文件名、章节、链接），保障可解释性 |
| **流式交互与会话记忆**   | 基于 SSE 的流式输出，首字延迟优化；MongoDB 管理多轮对话历史、实体关联与操作日志 |
| **高可用分层架构**       | 配置层、业务处理层、工具层、基础设施层清晰解耦，支持后台管理、多租户、行程规划等平滑扩展 |

### 1.4 角色说明
| 角色         | 核心诉求                                                     | 系统交互方式                                                 |
| ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **游客**     | 查询景点介绍、线路规划、酒店/美食/交通指南、实时实用信息     | 前端 Web/小程序对话界面，支持单轮/多轮问答、流式输出、引用点击查看 |
| **导游**     | 快速定位景区资料、回答常见问题、检索实用信息（开放时间/门票/注意事项） | 对话界面 + 快捷检索面板，支持按地区/类型/实体过滤            |
| **旅游运营** | 上传攻略文档、管理知识库内容、校验导入结果、监控任务状态     | 后台管理界面（中期规划），当前通过 API/CLI 上传，查看 MongoDB 任务看板 |

---

## 2. 系统架构

### 2.1 技术栈选型
| 类别         | 技术选型                             | 说明                                                       |
| ------------ | ------------------------------------ | ---------------------------------------------------------- |
| 工作流引擎   | LangGraph                            | 状态图编排，支持条件路由、并发分叉、流式输出与异步任务     |
| 向量数据库   | Milvus 2.4+                          | 支持稠密+稀疏向量混合搜索，元数据过滤（地区/类型/实体）    |
| 文档数据库   | MongoDB 6.0+                         | 存储对话历史、导入任务状态、实体映射、操作日志与反馈数据   |
| 对象存储     | MinIO                                | 存储原始文档、解析产物、图片/视频素材备份与调试快照        |
| 嵌入模型     | BGE-M3                               | 稠密向量（语义）+ 稀疏向量（关键词），支持多语言与长文本   |
| 重排序模型   | BGE-Reranker-Large / DashScope API   | 对多路召回结果二次精排，提升旅游垂直领域相关性             |
| 大语言模型   | Qwen 系列（阿里云 DashScope）        | 实体识别、意图分类、查询改写、HyDE 生成、流式答案生成      |
| 视觉语言模型 | Qwen-VL-Max                          | 解析景区实拍、地图、菜单、行程单图片，生成结构化文字描述   |
| 文档解析     | MinerU / Unstructured                | PDF/Word 转 Markdown，保留表格、行程单、图片引用与层级结构 |
| 实时信息     | 阿里云 MCP WebSearch / 天气&门票 API | 补充实时票价、开放时间、天气预警、交通管制等动态信息       |
| 配置管理     | Python Dataclass + .env              | 类型安全配置，环境隔离，支持热更新提示与单元测试 Mock      |
| 流式通信     | Server-Sent Events (SSE)             | 支持分块输出、引用标记同步推送、中断恢复与心跳保活         |

### 2.2 核心模块目录结构
```
tourism_kb/
├── config/                    # 配置层（Dataclass 模式）
│   ├── milvus_config.py
│   ├── minio_config.py
│   ├── mongo_config.py
│   ├── lm_config.py
│   ├── embedding_config.py
│   ├── reranker_config.py
│   ├── parser_config.py
│   ├── mcp_api_config.py
│   └── streaming_config.py
├── processor/                 # 业务处理层
│   ├── import_processor/      # 文档导入工作流
│   │   ├── nodes/             # 8个处理节点
│   │   ├── state.py           # ImportGraphState
│   │   └── main_graph.py      # TourismImportWorkflow
│   └── query_processor/       # 查询问答工作流
│       ├── nodes/             # 8个处理节点
│       ├── state.py           # QueryGraphState
│       └── main_graph.py      # TourismQueryWorkflow
├── utils/                     # 工具层
│   ├── milvus_utils.py
│   ├── embedding_utils.py
│   ├── reranker_http_utils.py
│   ├── mongo_history_utils.py
│   ├── mongo_task_utils.py
│   ├── minio_utils.py
│   └── citation_utils.py
└── tool/                      # 基础设施
    ├── logger.py
    └── stream_handler.py
```

### 2.3 架构分层与数据流向
```
[前端/客户端] ←SSE/HTTP→ [API Gateway] ←→ [LangGraph 工作流引擎]
                                              ├─ 导入工作流 → MinerU → VL → LLM抽取 → BGE-M3 → Milvus/MinIO/Mongo
                                              └─ 查询工作流 → LLM意图确认 → 多路检索(Milvus+MCP) → RRF+Rerank → LLM生成 → SSE返回
                                              ↓
                                          [数据存储层] Milvus(向量) + MongoDB(元数据/历史) + MinIO(文件)
```

---

## 3. 功能需求详解

### 3.1 文档导入工作流（Import Workflow）

#### 3.1.1 工作流程图
```
node_entry → [条件路由]
    ├─ PDF → node_doc_parse → node_img_vl → node_tourism_split
    └─ MD   → node_img_vl → node_tourism_split
                                      ↓
                          node_entity_metadata_extract
                                      ↓
                            node_bge_embedding
                                      ↓
                  node_import_milvus + node_update_task_status → END
```

#### 3.1.2 节点功能说明
| 节点名称                    | 功能描述                                                     | 输入                        | 输出                                          | 异常处理/降级策略                                            |
| --------------------------- | ------------------------------------------------------------ | --------------------------- | --------------------------------------------- | ------------------------------------------------------------ |
| `NodeEntry`                 | 入口校验：检测文件类型、大小、权限，创建导入任务记录（MongoDB） | `file_path`, `operator_id`  | `task_id`, `is_parse_enabled`                 | 文件超限/格式不支持 → 标记 `failed`，返回明确错误码          |
| `NodeDocParse`              | 文档解析：调用 MinerU 转 Markdown，保留行程表格、票价列表、图片占位符 | `file_path`                 | `md_path`, `md_content`, `parse_meta`         | 解析超时/失败 → 记录错误日志，任务状态 `partial`，跳过当前文件 |
| `NodeImgVL`                 | 图片语义理解：VL 模型解析景区实拍、地图、菜单，生成文字描述并嵌入对应章节 | `md_content`                | 增强后的 `md_content`（含 `[IMG_DESC]` 标记） | VL 服务不可用 → 保留原图片占位符，记录警告，不阻塞流程       |
| `NodeTourismSplit`          | 旅游感知切分：<br>1. 按标题/行程天数初切<br>2. 超长攻略二次切分<br>3. 短片段合并<br>4. 附加元数据占位符 | `md_content`, `source_file` | `chunks` (List[Dict])                         | 切分异常 →  fallback 至固定长度切分（500 tokens，重叠 50）   |
| `NodeEntityMetadataExtract` | 实体与元数据抽取：LLM 识别内容类型、景区/线路/酒店/餐厅名、地区、来源链接，标准化命名 | `chunks`, `md_content`      | 填充完整元数据的 `chunks`                     | LLM 抽取失败 → 标记 `unknown`，进入人工复核队列，不中断入库  |
| `NodeBGEEmbedding`          | 向量化：BGE-M3 生成稠密+稀疏向量，支持 GPU FP16 加速         | `chunks`                    | `dense_vector`, `sparse_vector`               | 显存不足 → 自动降级 CPU 推理，分批处理（batch_size=16）      |
| `NodeImportMilvus`          | 入库：写入 `kb_chunks` 与 `kb_tourism_entities`，建立实体倒排索引 | `chunks` with vectors       | Milvus 持久化结果                             | 写入失败 → 重试 3 次（指数退避），失败 chunk 写入死信表      |
| `NodeUpdateTaskStatus`      | 任务状态更新：写入 MongoDB `import_tasks`，触发进度推送/失败告警 | `task_id`, `stats`          | 任务状态流转（success/failed/partial）        | 状态写入失败 → 本地日志兜底，后台定时补偿同步                |

#### 3.1.3 关键设计点
- **旅游感知切分策略**：优先识别 `# 景区名称`、`## Day1/Day2`、`### 酒店/美食/交通` 等旅游文档常见结构；跳过伪标题（如票价表格内的 `#`）；同景区短段落自动合并，避免碎片化召回。
- **元数据强制校验**：每个 chunk 必须包含 `content_type`（枚举：景点介绍/线路推荐/酒店信息/美食推荐/交通指南/文化民俗）、`region`、`source_file`。缺失时触发 LLM 二次推断或标记为 `unknown` 进入人工复核队列。
- **异步任务隔离**：导入流程独立于查询服务，失败仅影响当前任务。通过 MongoDB 任务看板提供进度查询、失败原因、重试入口与人工干预接口。

---

### 3.2 查询问答工作流（Query Workflow）

#### 3.2.1 工作流程图
```
node_entity_intent_confirm → [条件路由]
    ├─ 有 answer（反问/拒识/直接回复）→ node_stream_answer → END
    └─ 需检索 → node_multi_search（并发分叉）
                    ├─→ node_search_embedding（稠密+稀疏混合检索）
                    ├─→ node_search_embedding_hyde（HyDE 假设答案检索）
                    └─→ node_web_search_mcp（实时天气/门票/交通）
                              ↓
                        node_join（合并去重）
                              ↓
                          node_rrf（RRF 融合排序）
                              ↓
                        node_rerank（重排序 Top-K）
                              ↓
                  node_citation_answer_gen（带引用流式生成）
                              ↓
                        node_save_history → END
```

#### 3.2.2 节点功能说明
| 节点名称                  | 功能描述                                                     | 输入                                         | 输出                                                         | 异常处理/降级策略                                        |
| ------------------------- | ------------------------------------------------------------ | -------------------------------------------- | ------------------------------------------------------------ | -------------------------------------------------------- |
| `NodeEntityIntentConfirm` | 旅游实体与意图确认：提取景区/线路/酒店等实体，识别意图，改写问题，分支处理 | `session_id`, `original_query`               | `entities`, `intent`, `rewritten_query`, `answer`(可选), `history` | LLM 超时 → 降级为关键词提取+规则意图匹配                 |
| `NodeSearchEmbedding`     | 混合向量检索：基于改写问题+实体过滤，在 `kb_chunks` 中执行 Dense+Sparse 搜索 | `rewritten_query`, `entities`, `intent`      | `embedding_chunks`                                           | Milvus 不可用 → 返回空列表，触发降级提示                 |
| `NodeSearchEmbeddingHyde` | HyDE 检索：LLM 生成假设性旅游攻略/答案，向量化后检索真实文档 | `rewritten_query`, `intent`                  | `hyde_embedding_chunks`                                      | HyDE 生成失败 → 跳过该路，仅依赖主检索                   |
| `NodeWebSearchMcp`        | 实时信息检索：调用 MCP 获取最新门票、开放时间、天气、交通管制 | `rewritten_query`, `entities`                | `web_docs`                                                   | API 限流/失败 → 返回空，答案生成时标注“实时信息暂不可用” |
| `NodeRrf`                 | RRF 融合：多路结果按倒排秩加权融合，去重并保留来源路径       | 多路 chunks                                  | `rrf_chunks`                                                 | 仅单路有结果 → 直接透传，不执行融合                      |
| `NodeRerank`              | 重排序：Reranker 对 RRF 结果精排，按意图动态调整 Top-K       | `rrf_chunks`, `rewritten_query`              | `reranked_docs`                                              | Reranker 服务异常 → 降级使用 RRF 原始排序                |
| `NodeCitationAnswerGen`   | 引用感知流式生成：组装 Prompt，强制输出 `[ref:id]` 引用标记，SSE 流式返回 | `reranked_docs`, `history`, `original_query` | 流式 `answer_chunks`, `citations`                            | LLM 中断 → 返回已生成片段+断点标识，支持前端续传         |
| `NodeSaveHistory`         | 历史持久化：保存用户问题、助手回复、关联实体、引用列表至 MongoDB | 完整对话数据                                 | `message_ids`                                                | 写入失败 → 异步重试，不影响本次响应返回                  |

#### 3.2.3 核心算法与机制详解

**1. 旅游实体与意图确认流程（8步）**
- 步骤1：参数校验（`session_id`, `original_query`）
- 步骤2：拉取最近 N 条历史对话（MongoDB）
- 步骤3：保存用户当前消息（状态标记为 `pending_resolution`）
- 步骤4：LLM 联合抽取实体与意图
  - 输入：当前问题 + 历史上下文
  - 输出：`{"entities": {"spot": ["天涯海角"], "region": "三亚"}, "intent": "practical_info", "rewritten_query": "三亚天涯海角景区门票价格与开放时间"}`
- 步骤5：实体向量对齐
  - 对抽取实体生成向量，在 `kb_tourism_entities` 中混合搜索
  - 返回 Top-N 标准化实体及置信度
- 步骤6：四级对齐规则
  - a. 唯一高分（>0.85）→ 直接确认
  - b. 多个高分 → 优先匹配原名/同地区，否则取最高分
  - c. 无高分但有中分（≥0.65）→ 返回前3候选，触发澄清反问
  - d. 无中分 → 拒识或降级为泛旅游攻略检索
- 步骤7：分支路由
  - 已确认 → 进入多路检索
  - 候选 → 生成反问：“您想查询的是以下哪个景区：A. 天涯海角 B. 南山文化旅游区？”
  - 拒识 → 返回引导话术+热门目的地推荐
- 步骤8：更新历史消息实体关联与意图标签

**2. RRF 融合算法**
```python
# score = Σ (weight_i / (k + rank_i)), k=60
# 旅游场景权重建议：embedding=1.0, hyde=0.8, web=0.6（实时信息降权防幻觉）
```

**3. HyDE 检索机制**
- 用户问题：“带老人孩子适合去哪玩？”
- LLM 生成假设答案：“适合亲子与老人的目的地通常具备：平缓步道、无障碍设施、医疗点近、节奏慢。例如三亚亚龙湾、厦门鼓浪屿…”
- 向量化假设答案 → 检索真实攻略文档 → 提升语义泛化召回率

**4. 引用溯源与流式输出设计**
- 每个 chunk 携带 `source_file`, `source_url`, `chunk_id`
- LLM Prompt 强制要求：`请在回答中使用 [ref:1]、[ref:2] 标注信息来源，不得编造未提供的内容。`
- SSE 推送格式：
  ```json
  {"type": "text", "content": "天涯海角门票为"}
  {"type": "citation", "ref_id": 1, "source": "三亚亲子游攻略.pdf", "url": "..."}
  {"type": "text", "content": "80元/人[ref:1]。"}
  {"type": "done", "citations": [{"id":1, "title":"...", "url":"..."}]}
  ```
- 支持中断恢复：记录 `stream_offset`，断线重连可续传

---

## 4. 数据存储设计

### 4.1 Milvus 向量数据库
**集合1：`ly_chunks`（旅游知识切片）**

| 字段名            | 类型                | 说明                                                  |
| ----------------- | ------------------- | ----------------------------------------------------- |
| `id`              | INT64               | 主键                                                  |
| `chunk_id`        | VARCHAR             | Chunk 唯一标识                                        |
| `content`         | VARCHAR             | 文本内容（含图片描述标记）                            |
| `content_type`    | VARCHAR             | 景点介绍/线路推荐/酒店信息/美食推荐/交通指南/文化民俗 |
| `spot_name`       | VARCHAR             | 景区名称（可空）                                      |
| `route_name`      | VARCHAR             | 线路名称（可空）                                      |
| `hotel_name`      | VARCHAR             | 酒店名称（可空）                                      |
| `restaurant_name` | VARCHAR             | 餐厅名称（可空）                                      |
| `region`          | VARCHAR             | 地区/城市                                             |
| `source_file`     | VARCHAR             | 来源文件名                                            |
| `source_url`      | VARCHAR             | 来源路径或资源链接                                    |
| `dense_vector`    | FLOAT_VECTOR(1024)  | BGE-M3 稠密向量                                       |
| `sparse_vector`   | SPARSE_FLOAT_VECTOR | BGE-M3 稀疏向量                                       |

**索引策略**：
- `dense_vector`：HNSW，COSINE
- `sparse_vector`：SPARSE_INVERTED_INDEX，IP
- 标量索引：`content_type`, `region`, `spot_name` 建立倒排索引，支持元数据过滤检索

**集合2：`ly_tourism_entities`（旅游实体库）**

| 字段名          | 类型                | 说明                                    |
| --------------- | ------------------- | --------------------------------------- |
| `id`            | INT64               | 主键                                    |
| `entity_name`   | VARCHAR             | 标准化实体名（景区/线路/酒店/餐厅）     |
| `entity_type`   | VARCHAR             | spot / route / hotel / restaurant       |
| `region`        | VARCHAR             | 归属地区                                |
| `aliases`       | JSON                | 别名列表（如“天涯海角/天涯海角游览区”） |
| `dense_vector`  | FLOAT_VECTOR(1024)  | 实体名向量                              |
| `sparse_vector` | SPARSE_FLOAT_VECTOR | 实体名稀疏向量                          |

### 4.2 MongoDB 文档数据库
**集合1：`chat_messages`（对话历史）**
```json
{
  "_id": "ObjectId",
  "session_id": "tour_session_001",
  "message_id": "msg_789012",
  "role": "user",
  "text": "三亚天涯海角门票多少钱？",
  "intent": "practical_info",
  "entities": {"spot": ["天涯海角"], "region": "三亚"},
  "rewritten_query": "三亚天涯海角景区门票价格与优惠政策",
  "citations": ["chunk_1024", "chunk_1055"],
  "timestamp": "2026-05-20T14:20:00Z"
}
```

**集合2：`import_tasks`（导入任务追踪）**
```json
{
  "task_id": "imp_20260520_001",
  "operator_id": "ops_user_01",
  "file_name": "三亚亲子游攻略.pdf",
  "status": "success", // pending / processing / success / failed / partial
  "progress": 1.0,
  "stats": {"total_chunks": 42, "success": 40, "failed": 2},
  "error_log": ["chunk_18: 实体抽取超时"],
  "created_at": "ISODate",
  "updated_at": "ISODate"
}
```

### 4.3 MinIO 对象存储
- Bucket：`tourism-kb`
- 目录结构：
  - `raw/`：原始上传文件
  - `parsed/`：转换后的 Markdown 与 JSON 元数据
  - `assets/`：景区图片、菜单、地图备份
  - `backup/`：chunks 快照与调试文件

---

## 5. 配置与工程规范

### 5.1 配置模式：Dataclass + 环境变量
统一采用类型安全配置模式，支持 IDE 补全、单元测试 Mock 与环境隔离。
```python
from dataclasses import dataclass
import os
from dotenv import load_dotenv

load_dotenv()

@dataclass
class TourismPromptConfig:
    entity_extract_prompt: str
    intent_classify_prompt: str
    hyde_gen_prompt: str
    answer_gen_prompt: str
    max_history_turns: int = 5

tourism_prompt_config = TourismPromptConfig(
    entity_extract_prompt=os.getenv("ENTITY_EXTRACT_PROMPT_PATH"),
    intent_classify_prompt=os.getenv("INTENT_CLASSIFY_PROMPT_PATH"),
    hyde_gen_prompt=os.getenv("HYDE_GEN_PROMPT_PATH"),
    answer_gen_prompt=os.getenv("ANSWER_GEN_PROMPT_PATH")
)
```

### 5.2 核心配置项
| 配置文件              | 主要用途            | 关键字段                                                     |
| --------------------- | ------------------- | ------------------------------------------------------------ |
| `milvus_config.py`    | Milvus 连接与集合名 | `milvus_url`, `chunks_collection`, `entities_collection`     |
| `minio_config.py`     | MinIO 连接          | `endpoint`, `access_key`, `bucket_name`                      |
| `mongo_config.py`     | MongoDB 连接        | `mongo_url`, `chat_db`, `task_db`                            |
| `lm_config.py`        | LLM API             | `base_url`, `api_key`, `entity_model`, `vl_model`, `llm_model`, `temperature` |
| `embedding_config.py` | 嵌入模型            | `bge_m3_path`, `bge_device`, `bge_fp16`                      |
| `reranker_config.py`  | 重排序              | `rerank_api_key`, `rerank_model`, `top_k`                    |
| `parser_config.py`    | 文档解析            | `mineru_url`, `api_token`, `max_file_size_mb`                |
| `mcp_api_config.py`   | 实时信息            | `mcp_base_url`, `weather_api_key`, `ticket_api_key`          |
| `streaming_config.py` | 流式输出            | `sse_timeout`, `chunk_size`, `enable_citation_stream`        |

### 5.3 日志与监控规范
- 全链路 Trace：每个请求分配 `trace_id`，贯穿导入/查询全节点
- 结构化日志：JSON 格式输出，包含 `node_name`, `duration_ms`, `input_tokens`, `output_tokens`, `status`
- 关键指标暴露：Prometheus 格式 `/metrics` 接口，包含 QPS、P95 延迟、Milvus 命中率、LLM 失败率
- 告警策略：连续 3 次 LLM 超时、Milvus 连接断开、导入任务失败率 > 10% 触发钉钉/邮件告警

---

## 6. 设计模式总结

| 模式             | 位置                                             | 说明                                                         | 优势                                     |
| ---------------- | ------------------------------------------------ | ------------------------------------------------------------ | ---------------------------------------- |
| **模板方法模式** | `processor/*/base.py`                            | `NodeBase` 定义 `__call__` 统一日志/异常，子类实现 `process()` | 减少重复代码，规范节点行为，便于统一埋点 |
| **状态模式**     | `state.py` (TypedDict)                           | `QueryGraphState` / `ImportGraphState` 传递上下文            | 类型安全，增量更新，LangGraph 原生兼容   |
| **单例模式**     | `utils/*_utils.py`                               | Milvus/Mongo/MinIO 客户端全局复用                            | 避免连接泄漏，提升并发性能               |
| **观察者模式**   | `tool/stream_handler.py` & `mongo_task_utils.py` | 任务进度/流式输出通过事件推送至前端                          | 解耦业务与通信层，支持实时反馈           |
| **策略模式**     | `query_processor/nodes/node_multi_search.py`     | 根据 `intent` 动态选择检索路数与权重                         | 灵活适配介绍/详情/实时查询等不同场景     |

---

## 7. 典型业务场景与交互流程

### 7.1 场景1：景点介绍（“三亚有哪些必去景点？”）
- **意图识别**：`intent = spot_recommendation`, `entities = {"region": "三亚"}`
- **检索策略**：向量检索 `content_type=景点介绍` + HyDE 生成假设推荐列表
- **返回内容**：景点列表、景区特色、适合人群、最佳游览时间，携带 `[ref:x]` 引用
- **交互形式**：流式输出，前端渲染为卡片列表，点击可展开详情

### 7.2 场景2：景点详情（“天涯海角开放时间？”）
- **意图识别**：`intent = spot_detail`, `entities = {"spot": "天涯海角", "region": "三亚"}`
- **检索策略**：实体过滤 + 稠密检索 + MCP 实时开放时间/门票查询
- **返回内容**：简介、开放时间、门票价格、游览路线、注意事项、实时动态
- **交互形式**：结构化摘要 + 详细段落，引用可跳转至原攻略 PDF 对应页

### 7.3 场景3：线路检索（“三亚5天4夜亲子游怎么安排？”）
- **意图识别**：`intent = route_search`, `entities = {"region": "三亚", "days": 5, "tag": "亲子"}`
- **检索策略**：向量检索 `content_type=线路推荐` + 行程天数元数据过滤
- **返回内容**：线路名、行程天数、每日安排、预算参考、交通方式、来源文件
- **交互形式**：时间轴视图渲染，支持按 Day 折叠/展开

### 7.4 场景4：实用信息（“带老人孩子适合去哪玩？”）
- **意图识别**：`intent = practical_info`, `entities = {"tag": ["老人", "亲子"]}`
- **检索策略**：HyDE 生成假设答案 → 检索真实攻略 → RRF 融合 → Reranker 精排
- **返回内容**：推荐目的地列表、无障碍设施说明、医疗点距离、节奏建议、Tips
- **交互形式**：流式图文混排，引用高亮，支持“换一批”或“细化条件”

### 7.5 多轮对话与模糊指代处理
- 用户：“门票多少钱？” → 系统检测历史上下文
- 若上文提及“天涯海角” → 自动补全实体继续检索
- 若历史无明确主体 → 触发澄清反问：
  > “您想查询的是以下哪个景区的门票信息？  
  > 1. 天涯海角游览区  
  > 2. 南山文化旅游区  
  > 3. 蜈支洲岛  
  > 请回复序号或完整名称。”

---

## 8. 关键技术指标与质量要求

### 8.1 性能指标
| 指标                | 目标值      | 说明                                    |
| ------------------- | ----------- | --------------------------------------- |
| 文档导入速度        | ≥ 15页/分钟 | 依赖解析复杂度与 GPU 性能，支持后台异步 |
| 查询响应时间（P95） | ≤ 2.5秒     | 含检索、排序、LLM 生成（非流式）        |
| 流式首字延迟        | ≤ 800ms     | SSE 首块返回时间                        |
| 向量检索延迟        | ≤ 150ms     | Milvus 混合搜索 + 元数据过滤            |
| 并发支持            | ≥ 50 QPS    | 依赖 Milvus 集群与 LLM 限流策略         |

### 8.2 准确性与质量指标
| 指标                 | 目标值  | 说明                               |
| -------------------- | ------- | ---------------------------------- |
| 实体/意图识别准确率  | ≥ 93%   | 景区/线路/酒店/地区抽取 + 意图分类 |
| 检索召回率（Top-10） | ≥ 88%   | 包含正确答案或强相关片段           |
| 引用溯源准确率       | ≥ 95%   | 答案中引用标记与实际来源严格对应   |
| 答案满意度           | ≥ 85%   | 用户点赞/有效反馈比例              |
| 系统可用性           | ≥ 99.5% | 导入失败隔离、降级检索、断流恢复   |

### 8.3 质量保障要求
- **异常隔离**：导入失败不阻塞查询服务；单节点失败触发降级策略，不导致全链路崩溃
- **可解释性**：所有答案必须携带引用来源；支持点击查看原文片段
- **可追溯性**：全链路日志记录 `trace_id`；对话历史完整保存实体、意图、引用映射
- **实时性**：流式输出心跳保活 5s；断线重连支持续传；MCP 实时信息缓存 TTL 10 分钟

---

## 9. 部署与环境要求

### 9.1 硬件要求
| 组件 | 最低配置       | 推荐配置                           |
| ---- | -------------- | ---------------------------------- |
| CPU  | 4核            | 8核+                               |
| 内存 | 16GB           | 32GB+                              |
| GPU  | 无（CPU 推理） | RTX 4060 8GB+（VL/Embedding 加速） |
| 磁盘 | 100GB          | 500GB SSD（MinIO + Milvus 数据）   |

### 9.2 软件依赖
- Python 3.10+
- Milvus 2.4+（Standalone 或 Cluster）
- MongoDB 6.0+
- MinIO（Latest）
- Redis（可选，用于流式缓冲与高频查询缓存）

### 9.3 环境变量配置（`.env` 示例）
```env
# LLM & API
DASHSCOPE_API_KEY=sk-xxx
LLM_DEFAULT_MODEL=qwen-max
VL_MODEL=qwen-vl-max

# Vector & DB
MILVUS_URL=http://localhost:19530
MONGO_URL=mongodb://localhost:27017
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Embedding & Rerank
BGE_M3_PATH=BAAI/bge-m3
BGE_DEVICE=cuda:0
BGE_FP16=true
RERANK_MODEL=bge-reranker-large
RERANK_TOP_K=6

# Tourism Specific
ENTITY_EXTRACT_PROMPT_PATH=./prompts/entity_extract.txt
ANSWER_GEN_PROMPT_PATH=./prompts/citation_answer.txt
MCP_WEATHER_API_KEY=xxx
MCP_TICKET_API_KEY=xxx
STREAM_CHUNK_SIZE=16
```

---

## 10. 演进路线与扩展规划

### 10.1 短期优化（1-3个月）
- [ ] 后台管理与知识库管理页面（文档上传、任务监控、元数据编辑、人工复核队列）
- [ ] 文档增量更新能力（基于文件 Hash 与 chunk 指纹对比，仅更新变动部分）
- [ ] 流式输出优化与 Redis 缓存层（缓存高频景点/线路问答，降低首字延迟）
- [ ] 异常重试与死信队列可视化看板

### 10.2 中期优化（3-6个月）
- [ ] 权限控制与多租户支持（RBAC，景区/旅行社数据隔离，操作审计日志）
- [ ] 行程规划与智能推荐（基于用户偏好、天数、预算生成结构化行程单，支持导出 PDF）
- [ ] 反馈学习机制（点赞/点踩/修改建议 → 动态调整 RRF 权重与 Reranker 微调）
- [ ] 多语言支持（跨境旅游场景：中英日韩泰，Prompt 与向量检索自适应）

### 10.3 长期优化（6-12个月）
- [ ] 旅游视频检索能力：基于视频字幕、讲稿、OCR 画面文本构建时间轴索引，返回 `视频标题/所属景区/开始时间/结束时间/片段摘要`
- [ ] 多模态联合检索：支持游客上传实拍图片/短视频，联合 VL 模型与向量库匹配景区/打卡点
- [ ] Agent 化旅游助手：对接订票、酒店、地图导航 API，实现“问答-规划-预订”闭环
- [ ] 知识图谱构建：景区-线路-交通-酒店关系图，支持推理型查询（如“从A景区到B酒店不坐船怎么走？”）

---

## 11. 附录

### 11.1 术语表
| 术语     | 解释                                                         |
| -------- | ------------------------------------------------------------ |
| HyDE     | Hypothetical Document Embeddings，假设性文档嵌入，用 LLM 生成假设答案提升召回 |
| RRF      | Reciprocal Rank Fusion，倒排秩融合算法，多路检索结果加权排序 |
| Reranker | 重排序模型，对初检结果进行交叉注意力精排                     |
| MCP      | Model Context Protocol，模型上下文协议，用于安全调用外部实时 API |
| Chunk    | 文档切分后的语义片段，携带旅游元数据与来源引用               |
| SSE      | Server-Sent Events，服务端推送事件，用于流式答案输出         |
| 旅游实体 | 景区、线路、酒店、餐厅、地区等垂直领域核心对象               |

### 11.2 参考文档
- LangGraph 官方文档：https://langchain-ai.github.io/langgraph/
- Milvus 文档：https://milvus.io/docs
- BGE-M3 论文：https://arxiv.org/abs/2402.03216
- 阿里云 DashScope API：https://help.aliyun.com/zh/dashscope/
- MinerU 文档：https://github.com/opendatalab/MinerU

### 11.3 版本历史
| 版本 | 日期       | 修改内容                                                     | 负责人      |
| ---- | ---------- | ------------------------------------------------------------ | ----------- |
| v1.0 | 2026-05-19 | 初始版本，基于掌柜智库架构迁移                               | 架构组      |
| v1.1 | 2026-05-20 | 校准旅游业务场景，补充异常处理、流式协议、数据字段、质量指标与演进路线 | 产品&架构组 |

---
**文档结束**  
如需获取可编辑的 `.md` 源文件，可直接复制本内容保存为 `旅游知识库智能问答系统_PRD_v1.1.md`。如需补充接口契约（OpenAPI/Swagger）、Prompt 模板明细或测试用例矩阵，请告知具体模块，我将为您继续输出。